@page
@model WebApp.Pages.Attendance.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Entry</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Entry.ClientId" class="control-label"></label>
                <input asp-for="Entry.ClientId" class="form-control" />
                <span asp-validation-for="Entry.ClientId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Entry.CourseId" class="control-label"></label>
                <input asp-for="Entry.CourseId" class="form-control" />
                <span asp-validation-for="Entry.CourseId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Entry.ScanTime" class="control-label"></label>
                <input asp-for="Entry.ScanTime" class="form-control" />
                <span asp-validation-for="Entry.ScanTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
