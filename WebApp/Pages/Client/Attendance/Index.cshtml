@page
@model WebApp.Pages.Client.Attendance.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutClient";
}

<h1>Attendance</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Entries[0].ClientId)
            </th> 
            <th> 
                @Html.DisplayNameFor(model => model.Entries[0].CourseId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entries[0].ScanTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Entries)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClientId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScanTime)
            </td>
        </tr>
}
    </tbody>
</table>
<p>To <a asp-page="/Client/ClientInterface">Welcome page</a>.</p>
